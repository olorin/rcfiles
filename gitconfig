[user]
    name = Sharif Olorin
    email = sio@tesser.org
    signingKey = 0x47D4CE7F6B9FDF57

[diff]
    tool = vimdiff

[merge]
    tool = vimdiff

[push]
    default = simple

[core]
    excludesfile = /home/sio/.gitignore_global

[credential]
    helper = cache

[alias]
    lg = log --pretty=format:'%Cred%h %Cgreen%aI %C(yellow)%G? %C(bold blue)<%ae> %Creset%s%C(yellow)%d' --abbrev-commit
    c = commit
    ca = commit -a
    tg = tag -s -u 6B9FDF57
    a = add -uv
    fuh = commit --fixup HEAD
    rima = rebase -i master --autosquash --autostash
    pso = push --set-upstream origin
    rc = rebase --continue
    undo = reset --soft HEAD~1
    pl = pull --ff-only
    pf = push --force-with-lease
    shallow = clone --shallow-submodules --depth 1
    b = rev-parse --abbref-ref HEAD
    s = status --porcelain=1 --branch

[commit]
    gpgSign = 0
